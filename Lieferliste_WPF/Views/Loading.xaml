<Grid x:Class="Lieferliste_WPF.Views.Loading"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:conv="clr-namespace:El2Core.Converters;assembly=El2Core"
        xmlns:prism="http://prismlibrary.com/"
        prism:ViewModelLocator.AutoWireViewModel="True"
        mc:Ignorable="d"
        Height="450" Width="800">

    <Grid.Resources>
        <conv:BooleanToVisibilityConverter x:Key="boolToVis"/>
    </Grid.Resources>
 
    <Grid Background="Azure">
        <TextBlock HorizontalAlignment="Center" VerticalAlignment="Top" FontSize="30" Text="{Binding ContentViewTask.Status}"
                   Visibility="{Binding ContentViewTask.IsNotCompleted, Converter={StaticResource boolToVis}}"/>
        <StackPanel Visibility="{Binding ContentViewTask.IsFaulted, Converter={StaticResource boolToVis}}">
            <TextBlock HorizontalAlignment="Center" Text="{Binding ContentViewTask.Exception}"/>
            <TextBlock HorizontalAlignment="Center" Text="{Binding ContentViewTask.ErrorMessage}"/>
            <TextBlock HorizontalAlignment="Center" Text="{Binding ContentViewTask.InnerException}"/>
        </StackPanel>
    </Grid>
    <ContentControl Content="{Binding ContentViewTask.Result}"/>
</Grid>
